"""Pure rendering layer for the stadium-scoreboard look.

Everything in this package is Textual-free (rich-only) and returns plain
``rich.text.Text`` rows or style strings, so it can be unit-tested offline.
"""
